import { mdiAlert, mdiCheck } from '@quasar/extras/mdi-v7';

export const NOTIFY_OPTIONS = {
  negative: {
    color: 'negative',
    icon: mdiAlert,
  },
  positive: {
    color: 'positive',
    icon: mdiCheck,
  },
};

export default NOTIFY_OPTIONS;
